int birthdayCakeCandles(int ar_count, int* ar) {
    int i,temp=ar[0],blown=0;
    for(i=0;i<ar_count;i++)
    {
        if(ar[i]>temp)
        temp=ar[i];
    }
    for(i=0;i<ar_count;i++)
    {
        if(ar[i]==temp)
        blown++;
    }
        return blown;
}
