void plusMinus(int arr_count, int* arr) {
int i;
float plus=0,minus=0,zero=0;
for(i=0;i<arr_count;i++)
{
    if(arr[i]>0)
    plus++;
    else if(arr[i]<0)
    minus++;
    else 
    zero++;
}
printf("%f",(plus/arr_count));
printf("\n%f",(minus/arr_count));
printf("\n%f",(zero/arr_count));
}
